# NPC

The market is NPCs. The NPCs are winning. Paired to $SPY on Robinhood Chain.

Single-file site (`index.html`) + one serverless function (`api/spy.js`, live $SPY quote and weekly closes). Mascot in `assets/npc3d.js` (three.js).

Live: paired to $SPY on Robinhood Chain via Pons.
